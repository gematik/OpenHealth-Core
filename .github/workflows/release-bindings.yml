# SPDX-FileCopyrightText: Copyright 2025 - 2026 gematik GmbH
#
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# *******
#
# For additional notes and disclaimer from gematik and in case of changes by gematik,
# find details in the "Readme" file.

name: Bindings Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. 1.0.0)'
        required: false
        type: string

env:
  CARGO_TERM_COLOR: always

concurrency:
  group: bindings-release-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  kotlin-bindings:
    name: Kotlin bindings (${{ matrix.display_name }})
    runs-on: ${{ matrix.runner }}
    env:
      OUT_ROOT: ${{ github.workspace }}/artifacts/${{ matrix.display_name }}/generated/uniffi
      CARGO_TARGET_DIR: ${{ github.workspace }}/artifacts/${{ matrix.display_name }}/cargo
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
      RUST_BACKTRACE: "1"
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            display_name: linux-x86_64
            platform: linux
            arch: x86_64
            library_file: libhealthcard.so
            just_shell: ""
          - runner: windows-latest
            display_name: windows-x86_64
            platform: windows
            arch: x86_64
            library_file: healthcard.dll
            just_shell: "C:/Program Files/Git/bin/bash.exe"
          - runner: macos-26
            display_name: darwin-aarch64
            platform: darwin
            arch: aarch64
            library_file: libhealthcard.dylib
            just_shell: ""

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo builds
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ". -> ${{ env.CARGO_TARGET_DIR }}"

      - name: Install just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Set up Java (Linux only)
        if: runner.os == 'Linux'
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Ensure Android SDK platforms (Linux only)
        if: runner.os == 'Linux'
        run: |
          yes | "${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager" --sdk_root="${ANDROID_SDK_ROOT}" --licenses >/dev/null || true
          "${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager" --sdk_root="${ANDROID_SDK_ROOT}" "platforms;android-34" "build-tools;34.0.0"

      - name: Install cargo-ndk (Linux only)
        if: runner.os == 'Linux'
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-ndk

      - name: Add Rust Android targets (Linux only)
        if: runner.os == 'Linux'
        run: rustup target add aarch64-linux-android x86_64-linux-android

      - name: Set Perl environment variables
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $perl = (where.exe perl)[0]
          echo "PERL=$perl" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          echo "OPENSSL_SRC_PERL=$perl" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      - name: Setup MSVC (Windows only)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Remove conflicting link.exe (Windows only)
        if: runner.os == 'Windows'
        run: |
          if [ -x /usr/bin/link ]; then
            rm -f /usr/bin/link
          fi

      - name: Build Kotlin bindings
        run: |
          if [ -n "${{ matrix.just_shell }}" ]; then
            just --shell="${{ matrix.just_shell }}" kotlin-bindings-generate ${{ matrix.platform }} ${{ matrix.arch }} ${{ matrix.library_file }} release
          else
            just kotlin-bindings-generate ${{ matrix.platform }} ${{ matrix.arch }} ${{ matrix.library_file }} release
          fi

      - name: Build Android native libraries (Linux only - arm64-v8a, x86_64)
        if: runner.os == 'Linux'
        run: |
          just kotlin-bindings-generate-android

      - name: Upload OpenSSL Configure logs (on failure)
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: openssl-configure-logs-${{ matrix.display_name }}
          path: |
            ${{ env.CARGO_TARGET_DIR }}/**/configure.log
            ${{ github.workspace }}/**/configure.log
          if-no-files-found: warn

      - name: Upload Resources
        uses: actions/upload-artifact@v6
        with:
          name: kotlin-bindings-${{ matrix.display_name }}
          path: |
            ${{ env.OUT_ROOT }}/resources/**
            ${{ env.OUT_ROOT }}/kotlin/**
            ${{ env.OUT_ROOT }}/android-jni/**
          if-no-files-found: warn

  kotlin-asn1-bindings:
    name: Kotlin ASN.1 bindings (${{ matrix.display_name }})
    runs-on: ${{ matrix.runner }}
    env:
      OUT_ROOT: ${{ github.workspace }}/artifacts/asn1/${{ matrix.display_name }}/generated/uniffi
      CARGO_TARGET_DIR: ${{ github.workspace }}/artifacts/asn1/${{ matrix.display_name }}/cargo
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
      RUST_BACKTRACE: "1"
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            display_name: linux-x86_64
            platform: linux
            arch: x86_64
            library_file: libasn1.so
            just_shell: ""
          - runner: windows-latest
            display_name: windows-x86_64
            platform: windows
            arch: x86_64
            library_file: asn1.dll
            just_shell: "C:/Program Files/Git/bin/bash.exe"
          - runner: macos-26
            display_name: darwin-aarch64
            platform: darwin
            arch: aarch64
            library_file: libasn1.dylib
            just_shell: ""

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo builds
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ". -> ${{ env.CARGO_TARGET_DIR }}"

      - name: Install just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Set up Java (Linux only)
        if: runner.os == 'Linux'
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Ensure Android SDK platforms (Linux only)
        if: runner.os == 'Linux'
        run: |
          yes | "${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager" --sdk_root="${ANDROID_SDK_ROOT}" --licenses >/dev/null || true
          "${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager" --sdk_root="${ANDROID_SDK_ROOT}" "platforms;android-34" "build-tools;34.0.0"

      - name: Install cargo-ndk (Linux only)
        if: runner.os == 'Linux'
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-ndk

      - name: Add Rust Android targets (Linux only)
        if: runner.os == 'Linux'
        run: rustup target add aarch64-linux-android x86_64-linux-android

      - name: Set Perl environment variables
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $perl = (where.exe perl)[0]
          echo "PERL=$perl" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          echo "OPENSSL_SRC_PERL=$perl" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      - name: Setup MSVC (Windows only)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Remove conflicting link.exe (Windows only)
        if: runner.os == 'Windows'
        run: |
          if [ -x /usr/bin/link ]; then
            rm -f /usr/bin/link
          fi

      - name: Build Kotlin ASN.1 bindings
        run: |
          if [ -n "${{ matrix.just_shell }}" ]; then
            just --shell="${{ matrix.just_shell }}" kotlin-asn1-bindings-generate ${{ matrix.platform }} ${{ matrix.arch }} ${{ matrix.library_file }} release
          else
            just kotlin-asn1-bindings-generate ${{ matrix.platform }} ${{ matrix.arch }} ${{ matrix.library_file }} release
          fi

      - name: Build Android native libraries (Linux only - arm64-v8a, x86_64)
        if: runner.os == 'Linux'
        run: |
          just kotlin-asn1-bindings-generate-android

      - name: Upload OpenSSL Configure logs (on failure)
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: openssl-configure-logs-asn1-${{ matrix.display_name }}
          path: |
            ${{ env.CARGO_TARGET_DIR }}/**/configure.log
            ${{ github.workspace }}/**/configure.log
          if-no-files-found: warn

      - name: Upload Resources
        uses: actions/upload-artifact@v6
        with:
          name: kotlin-asn1-bindings-${{ matrix.display_name }}
          path: |
            ${{ env.OUT_ROOT }}/resources/**
            ${{ env.OUT_ROOT }}/kotlin/**
            ${{ env.OUT_ROOT }}/android-jni/**
          if-no-files-found: warn

  swift-bindings:
    name: Swift bindings (darwin-universal)
    runs-on: macos-26
    env:
      OUT_ROOT: ${{ github.workspace }}/artifacts/swift/generated/uniffi
      CARGO_TARGET_DIR: ${{ github.workspace }}/artifacts/swift/cargo
      RUST_BACKTRACE: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-ios, aarch64-apple-ios-sim, x86_64-apple-ios, aarch64-apple-darwin, x86_64-apple-darwin

      - name: Cache Cargo builds
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ". -> ${{ env.CARGO_TARGET_DIR }}"

      - name: Install just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Build XCFramework
        run: just swift-xcframework

      - name: Stage Swift wrapper for release
        run: cp core-modules-swift/healthcard/Sources/OpenHealthHealthcard/OpenHealthHealthcard.swift OpenHealthHealthcard.swift

      - name: Upload Swift wrapper
        uses: actions/upload-artifact@v6
        with:
          name: swift-wrapper
          path: OpenHealthHealthcard.swift
          if-no-files-found: error

      - name: Upload OpenSSL Configure logs (Swift - on failure)
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: openssl-configure-logs-swift
          path: |
            ${{ env.CARGO_TARGET_DIR }}/**/configure.log
            ${{ github.workspace }}/**/configure.log
          if-no-files-found: warn

      - name: Zip XCFramework
        working-directory: core-modules-swift/healthcard
        run: zip -r OpenHealthHealthcardFFI.xcframework.zip OpenHealthHealthcardFFI.xcframework

      - name: Write XCFramework checksum
        working-directory: core-modules-swift/healthcard
        run: |
          swift package compute-checksum OpenHealthHealthcardFFI.xcframework.zip > OpenHealthHealthcardFFI.xcframework.sha256

      - name: Upload XCFramework Zip
        uses: actions/upload-artifact@v6
        with:
          name: swift-xcframework
          path: core-modules-swift/healthcard/OpenHealthHealthcardFFI.xcframework.zip

      - name: Upload XCFramework checksum
        uses: actions/upload-artifact@v6
        with:
          name: swift-xcframework-checksum
          path: core-modules-swift/healthcard/OpenHealthHealthcardFFI.xcframework.sha256

  swift-asn1-bindings:
    name: Swift ASN.1 bindings (darwin-universal)
    runs-on: macos-26
    env:
      OUT_ROOT: ${{ github.workspace }}/artifacts/swift-asn1/generated/uniffi
      CARGO_TARGET_DIR: ${{ github.workspace }}/artifacts/swift-asn1/cargo
      RUST_BACKTRACE: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-ios, aarch64-apple-ios-sim, x86_64-apple-ios, aarch64-apple-darwin, x86_64-apple-darwin

      - name: Cache Cargo builds
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ". -> ${{ env.CARGO_TARGET_DIR }}"

      - name: Install just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Build XCFramework (ASN.1)
        run: just swift-asn1-xcframework

      - name: Stage Swift wrapper for release
        run: cp core-modules-swift/asn1/Sources/OpenHealthAsn1/OpenHealthAsn1.swift OpenHealthAsn1.swift

      - name: Upload Swift wrapper
        uses: actions/upload-artifact@v6
        with:
          name: swift-asn1-wrapper
          path: OpenHealthAsn1.swift
          if-no-files-found: error

      - name: Upload OpenSSL Configure logs (Swift ASN.1 - on failure)
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: openssl-configure-logs-swift-asn1
          path: |
            ${{ env.CARGO_TARGET_DIR }}/**/configure.log
            ${{ github.workspace }}/**/configure.log
          if-no-files-found: warn

      - name: Zip XCFramework
        working-directory: core-modules-swift/asn1
        run: zip -r OpenHealthAsn1FFI.xcframework.zip OpenHealthAsn1FFI.xcframework

      - name: Write XCFramework checksum
        working-directory: core-modules-swift/asn1
        run: |
          swift package compute-checksum OpenHealthAsn1FFI.xcframework.zip > OpenHealthAsn1FFI.xcframework.sha256

      - name: Upload XCFramework Zip
        uses: actions/upload-artifact@v6
        with:
          name: swift-asn1-xcframework
          path: core-modules-swift/asn1/OpenHealthAsn1FFI.xcframework.zip

      - name: Upload XCFramework checksum
        uses: actions/upload-artifact@v6
        with:
          name: swift-asn1-xcframework-checksum
          path: core-modules-swift/asn1/OpenHealthAsn1FFI.xcframework.sha256

  assemble-kotlin-bindings:
    name: Assemble Kotlin bindings bundle
    runs-on: ubuntu-latest
    needs: [kotlin-bindings, kotlin-asn1-bindings]
    steps:
      - name: Checkout (for directory layout)
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Gradle cache
        uses: gradle/actions/setup-gradle@v5

      - name: Download platform artifacts
        uses: actions/download-artifact@v7
        with:
          pattern: "kotlin-bindings-*"
          path: assembly/input

      - name: Download ASN.1 platform artifacts
        uses: actions/download-artifact@v7
        with:
          pattern: "kotlin-asn1-bindings-*"
          path: assembly/input-asn1

      - name: Collect libraries and generated Kotlin (healthcard + asn1)
        run: |
          just kotlin-bindings-assemble assembly/input assembly/dist/healthcard/generated/uniffi
          just kotlin-bindings-assemble assembly/input-asn1 assembly/dist/asn1/generated/uniffi

      - name: Upload Kotlin bindings bundle (healthcard)
        uses: actions/upload-artifact@v6
        with:
          name: kotlin-bindings-bundle
          path: |
            assembly/dist/healthcard/generated/uniffi/resources/**
            assembly/dist/healthcard/generated/uniffi/kotlin/**
            assembly/dist/healthcard/generated/uniffi/android-jni/**
          if-no-files-found: error

      - name: Upload Kotlin bindings bundle (asn1)
        uses: actions/upload-artifact@v6
        with:
          name: kotlin-asn1-bindings-bundle
          path: |
            assembly/dist/asn1/generated/uniffi/resources/**
            assembly/dist/asn1/generated/uniffi/kotlin/**
            assembly/dist/asn1/generated/uniffi/android-jni/**
          if-no-files-found: error

      - name: Publish Kotlin bindings to mavenLocal (assembled outputs)
        run: |
          if [ -n "${{ inputs.version }}" ]; then
            export ORG_GRADLE_PROJECT_version="${{ inputs.version }}"
          fi
          OUT_ROOT="${GITHUB_WORKSPACE}/assembly/dist/healthcard/generated/uniffi" just kotlin-publish-local
          OUT_ROOT="${GITHUB_WORKSPACE}/assembly/dist/asn1/generated/uniffi" just kotlin-asn1-publish-local

      - name: Stage Maven local artifacts for upload
        run: |
          set -euo pipefail
          upload_root="${GITHUB_WORKSPACE}/assembly/maven-repo"
          src_root="${HOME}/.m2/repository/de/gematik/openhealth"
          mkdir -p "${upload_root}/de/gematik"
          cp -a "${src_root}" "${upload_root}/de/gematik/"

      - name: Upload Maven local artifacts
        uses: actions/upload-artifact@v6
        with:
          name: kotlin-bindings-maven-local
          path: assembly/maven-repo
          if-no-files-found: error

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [swift-bindings, swift-asn1-bindings, assemble-kotlin-bindings]
    if: inputs.version != ''
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Download Swift Artifact
        uses: actions/download-artifact@v7
        with:
          name: swift-xcframework
          path: artifacts/swift

      - name: Download Swift wrapper
        uses: actions/download-artifact@v7
        with:
          name: swift-wrapper
          path: artifacts/swift-wrapper
      
      - name: Download Swift checksum
        uses: actions/download-artifact@v7
        with:
          name: swift-xcframework-checksum
          path: artifacts/swift-checksum

      - name: Download Swift ASN.1 Artifact
        uses: actions/download-artifact@v7
        with:
          name: swift-asn1-xcframework
          path: artifacts/swift-asn1

      - name: Download Swift ASN.1 wrapper
        uses: actions/download-artifact@v7
        with:
          name: swift-asn1-wrapper
          path: artifacts/swift-asn1-wrapper
      
      - name: Download Swift ASN.1 checksum
        uses: actions/download-artifact@v7
        with:
          name: swift-asn1-xcframework-checksum
          path: artifacts/swift-asn1-checksum

      - name: Download Kotlin Artifact
        uses: actions/download-artifact@v7
        with:
          name: kotlin-bindings-maven-local
          path: artifacts/kotlin

      - name: Zip Kotlin Maven Repo
        run: |
          cd artifacts/kotlin
          zip -r ../../maven-repo.zip .

      - name: Update Package.swift
        run: |
          VERSION="${{ inputs.version }}"
          CHECKSUM=$(cat artifacts/swift-checksum/OpenHealthHealthcardFFI.xcframework.sha256)
          REPO_URL="https://github.com/${{ github.repository }}/releases/download/${VERSION}/OpenHealthHealthcardFFI.xcframework.zip"

          mkdir -p core-modules-swift/healthcard/Sources/OpenHealthHealthcard
          cp artifacts/swift-wrapper/OpenHealthHealthcard.swift core-modules-swift/healthcard/Sources/OpenHealthHealthcard/OpenHealthHealthcard.swift

          ASN1_CHECKSUM=$(cat artifacts/swift-asn1-checksum/OpenHealthAsn1FFI.xcframework.sha256)
          ASN1_REPO_URL="https://github.com/${{ github.repository }}/releases/download/${VERSION}/OpenHealthAsn1FFI.xcframework.zip"

          mkdir -p core-modules-swift/asn1/Sources/OpenHealthAsn1
          cp artifacts/swift-asn1-wrapper/OpenHealthAsn1.swift core-modules-swift/asn1/Sources/OpenHealthAsn1/OpenHealthAsn1.swift
          
          # Update Package.swift to use remote binary target
          sed -i "s|path: \"core-modules-swift/healthcard/OpenHealthHealthcardFFI.xcframework\"|url: \"$REPO_URL\", checksum: \"$CHECKSUM\"|" Package.swift
          sed -i "s|path: \"core-modules-swift/asn1/OpenHealthAsn1FFI.xcframework\"|url: \"$ASN1_REPO_URL\", checksum: \"$ASN1_CHECKSUM\"|" Package.swift
          
          # Verify change
          cat Package.swift

          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add Package.swift
          git add -f core-modules-swift/healthcard/Sources/OpenHealthHealthcard/OpenHealthHealthcard.swift
          git add -f core-modules-swift/asn1/Sources/OpenHealthAsn1/OpenHealthAsn1.swift
          git commit -m "Release ${VERSION}"
          git tag "${VERSION}"
          git push origin "${VERSION}"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.version }}
          files: |
            artifacts/swift/OpenHealthHealthcardFFI.xcframework.zip
            artifacts/swift-asn1/OpenHealthAsn1FFI.xcframework.zip
            maven-repo.zip
          generate_release_notes: true
