# Crypto JVM Base Library

This module hosts all the compiled libs needed for the JVM part of the crypto module.
The android NDK is NOT part of this.
Android NDK supports cross compilation out of the box.
